import XCTest
import Nimble

@testable import ElementsList

final class ElementsListTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ElementsList().text, "Hello, World!")
    }
}
